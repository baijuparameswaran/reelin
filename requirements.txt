# reel — runtime dependencies (kept intentionally minimal for robustness).
# The Ollama client uses only the Python stdlib (urllib); the sole 3rd-party
# dependency is PyYAML for the human-editable model config.
PyYAML>=6.0

# PDF ingestion — used when the source file is a .pdf script / screenplay.
pypdf>=4.0

# Google GenAI SDK — used for Veo video generation and Gemini image generation.
# The urllib fallback in gemini.py remains active for environments where this
# package is unavailable, so the SDK is optional for the text pipeline.
google-genai>=1.0

